{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm exec tsc --build ./tsconfig.build.json --force && pnpm --filter epayco-store build",
  "outputDirectory": "example/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
